# 任务：UI 微调（第 1 轮）

项目根：`/Users/magicnevermore/Projects/sparkling`。先读 `CLAUDE.md` 建立背景。

只做 3 处修复，**不要扩散到其他文件**。完成后跑 `pnpm exec tsc --noEmit` 和 `pnpm build` 自检通过。

## 修复 1：GraphCanvas 视图模式切换体验

文件：`frontend/src/components/GraphCanvas.tsx`

**现状问题**：
- 用户单击节点 → 自动切到 focus 模式
- 但下拉里「聚焦」选项在 `!focusId` 时 disabled，导致进入 focus 后无法通过下拉返回全图（必须按 Reset 按钮）
- 「单击 = 进 focus」与"hover 已经能预览"功能重复

**改成**：
- 单击节点：仅高亮该节点（视觉上加 violet ring，比 hover 更明显），不改 mode
- 双击节点：进入 focus 模式（`mode='focus'`, `focusId=node.id`），不再跳详情页
- 节点上的"更多"或一个右上角 chevron：跳详情。或者在 hover 卡片里放一个「打开详情 →」链接
- 下拉始终可选「全图」「聚焦」（聚焦项不再 disabled，但若 focusId 为空选中聚焦时回退到全图）
- 主题模式保持 disabled

实现要点：
- 删掉 `onNodeClick` 里 `setMode('focus')`，改为 `setFocusId(node.id)`（仅高亮）
- 把 hover 浮层里的 `关联数 N` 那行下方加一个 `<Link to={\`/atoms/\${atom.id}\`}>打开 →</Link>`（用 react-router 的 Link；浮层需要 pointer-events-auto 才能点击 — 现在是 pointer-events-none，要去掉）
- ThoughtNode 内 `data.highlight` 已经处理高亮态；扩展它在 `focusId === id` 时也高亮

## 修复 2：AtomCard 用 Link 而非 button + navigate

文件：`frontend/src/components/AtomCard.tsx`

**现状**：第 31 行 `<button type="button" onClick={() => navigate(...)}>` 包内容

**改成**：用 `react-router-dom` 的 `<Link to={\`/atoms/\${atom.id}\`}>` 包内容，支持中键/Cmd+点击新窗口打开。删掉 `useNavigate` import。

注意：原来的 `<button>` 在 `<article>` 内部还有同级三点菜单按钮——保持外层结构不变，仅把"点击主体"的 button 换成 Link。Link 的 className 保持一致。

## 修复 3：时间字段改用 createdAt

文件：`frontend/src/lib/time.ts`（如有 groupAtomsByTime 函数）和 `frontend/src/pages/Inbox.tsx`、`frontend/src/components/AtomCard.tsx`

**现状**：分组与卡片右下角时间都用 `atom.updatedAt`

**改成**：全部改用 `atom.createdAt`（"想法发生的时间"更符合直觉，编辑不应让条目跳到顶部）

具体：
- `Inbox.tsx`：`groupAtomsByTime(atoms)` 内部按 createdAt 排序与分组
- `AtomCard.tsx`：`formatRelative(atom.createdAt)` 替换 updatedAt
- `time.ts` 若 groupAtomsByTime 写在这里，改成读 createdAt

## 自检

```bash
cd frontend
pnpm exec tsc --noEmit
pnpm build
```

## 输出

完成后用中文一句话总结：每处改了哪些行，是否有偏差。不要碰别的文件。
