# 任务：实现 Sparkling 前端 UI（Mock 版）

## 角色与背景
你是协作开发者。**项目根目录是 `/Users/magicnevermore/Projects/sparkling`**。先读以下两份文档建立背景：
- `CLAUDE.md` — 项目级约定（必读：编码风格、设计 token、关键约定）
- `PLAN.md` — 完整实施计划与数据模型

后端骨架已就绪但**本次任务不碰后端**，只用 mock data 实现纯前端。前端目录为 `frontend/`，已用 pnpm 装好依赖（react-router-dom / zustand / reactflow / tailwindcss / vite-plugin-pwa）。`pnpm build` 当前已能通过。

## 总目标
实现完整的 5 个页面 + 布局系统，使用 mock data，让 `pnpm dev` 跑起来后可以浏览全部交互（除真实 AI 调用外）。所有"日后接入真实后端"的位置用 `// TODO(real-api):` 标记。

## 设计 Token（必须严格遵守）

```
背景:     bg-slate-950 主背景 / bg-slate-900 卡片面板 / bg-slate-800 hover
边框:     border-slate-800 默认 / border-slate-700 强调
文字:     text-slate-100 主 / text-slate-400 次 / text-slate-500 辅
强调:     violet-400 (#a78bfa) — AI 关联、CTA、焦点环
确认:     emerald-400 — 已确认关联 (实线绿)
警告:     amber-400 — 冲突 / version 错位
危险:     rose-500 — 删除 / 重建 embedding
圆角:     rounded-xl 卡片 / rounded-md 按钮输入
字体:     Inter (英文) + 系统中文 + JetBrains Mono (代码) — 直接通过 system fallback，无需引入字体文件
间距:     用 Tailwind 1/2/3/4/6 (4/8/12/16/24 px)
```

## 布局系统（三档断点）

| 断点 | 形态 | 备注 |
| - | - | - |
| `lg:` (≥ 1024px) | 左 Sidebar 240px，含「最近」列表 | 主导航 + 最近 5 条想法 |
| `md:` (768–1024) | 左 64px icon 栏 | hover 显示 tooltip，不显示「最近」 |
| 默认 (< 768px) | 顶部仅状态点 + 底部 Tab Bar | 移动端肌肉记忆 |

实现要点：
- 用 Tailwind 的 `lg:flex` `md:hidden` 等条件渲染，**不要写 JS 媒体查询**
- Sidebar 与 BottomTabBar 共享同一份 `navItems` 数组
- 选中态：`bg-slate-900` + 左侧 2px violet 强调条（移动端是顶部 2px）
- Sidebar 顶部：✨ Logo + 连接状态点（绿/黄/灰圆点 + 文字"在线/重连中/离线"）
- Sidebar「最近」区：取 mockAtoms 前 5 条，每条显示 `truncate` 后的内容，点击跳详情

## Mock Data 设计 (`src/lib/mock.ts`)

```typescript
// TODO(real-api): 整个文件由 lib/api.ts 真实调用替换；保留类型定义供组件使用
export interface AtomMock { id: string; content: string; status: 'inbox'|'active'|'archived'; version: number; createdAt: string; updatedAt: string; }
export interface LinkMock { id: string; fromAtomId: string; toAtomId: string; confidence: number; source: 'ai_auto'|'ai_suggested'|'user'; userConfirmed: boolean; }
```

**Mock 内容**（15 条想法分四簇）：
1. 晨跑/锻炼簇 (5 条)：节奏感、心率区间、配速、出汗与脱水、晨跑 vs 夜跑
2. 工作/专注簇 (4 条)：番茄钟、Deep Work、上下文切换、专注力恢复
3. 读书/写作簇 (3 条)：写作肌肉记忆、阅读速率、笔记复习
4. 散点 (3 条)：随手记录的无关想法（如「咖啡机坏了」「周末计划」「梦到老同学」）

**Mock 关联** (20 条)：簇内全连接 + 几条跨簇弱连接。分布：5 条 ai_auto+confirmed / 3 条 user+confirmed / 12 条 ai_suggested+未确认。confidence 在 0.62–0.95 之间合理散布。

时间戳分布：刚刚 / 几分钟前 / 几小时前 / 昨天 / 几天前，让 Inbox 的时间分组都能看到。

**mockApi 封装**：
```typescript
const delay = (ms: number) => new Promise(r => setTimeout(r, ms));
export const mockApi = {
  listAtoms: async () => { await delay(120); return [...atoms]; },
  getAtom: async (id) => { await delay(80); ... },
  createAtom: async (content) => { ... 推入 atoms[0], 返回新 atom ... },
  updateAtom: async (id, patch, version) => { ... 校验 version 并 +1，否则抛 ConflictError ... },
  listLinks: async () => { ... },
  confirmLink: async (id) => { ... },
  ignoreLink: async (id) => { ... },
  search: async (q) => { ... 简单 substring 匹配 + 模拟 score ... },
};
```

## Mock WS 模拟 (`src/lib/useMockWs.ts`)

每 8–15 秒随机：
- 30% 概率：在「散点」簇里插一条新的"AI 建议"关联（与已有节点 random pick）
- 推送通过 Zustand store 更新状态，触发 UI 重渲染
- 建议条目首次出现时带 `animate-in fade-in slide-in-from-right` 动画（用 Tailwind 内置或简单 CSS）

提供 `useMockWs()` hook，在 `App.tsx` 顶层调用一次启动 interval。组件 unmount 时清理。

## Zustand Store (`src/lib/store.ts`)

```typescript
interface State {
  atoms: AtomMock[];
  links: LinkMock[];
  wsStatus: 'online' | 'reconnecting' | 'offline';
  // actions
  loadInitial: () => Promise<void>;
  addAtom: (content: string) => Promise<void>;
  updateAtom: (id: string, patch: Partial<AtomMock>) => Promise<void>;
  confirmLink: (id: string) => Promise<void>;
  ignoreLink: (id: string) => Promise<void>;
  pushSuggestion: (link: LinkMock) => void;  // mock WS 用
}
```

启动时 `loadInitial()` 从 mockApi 拉数据；wsStatus 初值 `online`，每隔 60s 切到 reconnecting 1s 后回 online，模拟视觉效果。

## 页面规约

### 1. Inbox (`src/pages/Inbox.tsx`)

- 顶部 QuickInput 卡片：`textarea` 自动高度（用 `field-sizing-content` 或简单 ref + scrollHeight），placeholder「💭 把脑子里的东西扔进来…」，**Cmd/Ctrl+Enter 提交**，提交后清空保持焦点，右下角显示 `Cmd+Enter` 灰字提示
- 按时间分组的卡片流：分组标题「刚刚 / N 分钟前 / 今天 / 昨天 / YYYY-MM-DD」(`text-slate-500 text-xs uppercase`)
- 卡片 `AtomCard`：内容最多 4 行 `line-clamp-4`，下方 badge 行（`LinkBadge` 紫色=建议数，绿色=已确认数），右上角 3-dot 菜单（暂时只显示 disabled 项 "归档/删除"，无实际逻辑）
- 点击卡片主体跳 `/atoms/:id`
- 空态：当 atoms 为空时显示插画占位（用纯 emoji + 文字即可，如 `🌱 + "把今天第一个想法记下来"`）

### 2. AtomDetail (`src/pages/AtomDetail.tsx`)

桌面端两栏布局（左 2fr / 右 1fr），移动端单列堆叠：
- 左：返回按钮 + 大字号内容（可点击进入编辑：`contentEditable` 或切到 textarea，失焦自动调用 `updateAtom`），底部元信息行（创建时间 / 状态 / version）
- 右：「已确认 (N)」+ 列表（绿色 dot + 想法标题，点击跳转）；「AI 建议 (N)」+ `LinkSuggest` 条目（紫色 dot + 标题 + `SimilarityBar` 百分比条 + 双按钮 Confirm/Ignore；按钮点击调用 store action，UI 立即移除条目）
- 模拟 version 冲突：当 `updateAtom` 抛 ConflictError 时显示 amber toast「版本冲突，已重新加载最新内容」

### 3. Graph (`src/pages/Graph.tsx`)

- 全屏 React Flow 画布（高度撑满 main area）
- 节点：自定义节点组件，圆形（用 div + `rounded-full` 包内容首 20 字），半径按 degree 缩放（基础 64px，每多 1 个关联 +6px，上限 120px），degree ≥ 5 用 violet 边，否则 slate
- 边：`user_confirmed=true` 实线 emerald；否则虚线 violet（`strokeDasharray`）
- 工具栏（左上角 floating）：视图切换 `[全图 ▾ / 聚焦]`、Reset、适应屏幕（React Flow 自带 `fitView`）
- 右上角搜索：输入 query → 高亮匹配节点 + zoom 到该节点
- hover 节点：浮层显示前 80 字 + 关联数
- 双击节点跳 `/atoms/:id`
- 单击节点：进入「聚焦模式」，过滤为该节点 + 一/二度邻居
- 主题模式不实现（Phase 2，显示 `disabled` 的 tab 即可）

### 4. Search (`src/pages/Search.tsx`)

- 顶部大搜索框 + placeholder「🔍 语义搜索：试试 "晨跑 心率"…」
- input debounced 300ms，调 `mockApi.search`
- 结果计数 + 结果卡片（复用 `AtomCard` + 追加 `SimilarityBar`）
- 空 query 显示提示「按相似度返回结果。试试关键词组合。」
- 无结果显示空态

### 5. Settings (`src/pages/Settings.tsx`)

仅 UI（保存按钮调用 mock 函数 + toast 成功，无真实持久化）：

- **AI Provider 卡片**：Base URL / API Key (password) / Embed Model / **Embed Dim 下拉**（常用值 384/768/1024/1536/3072）/ Chat Model / 「测试连接」按钮（mock：随机 80% 成功 / 20% 失败 toast）
- **关联阈值卡片**：两个 range slider（自动确认 0–1, step 0.01；建议门槛 0–1）+ 实时数值显示，自动 ≥ 建议 + 0.05 否则置灰
- **危险操作卡片**：rose-500 边框，「重建 embedding」按钮 → 弹 `ConfirmDialog`（"将重新生成全部 N 条想法的向量..."）→ mock 一个 fake 进度条（用 setInterval 0→100%，约 6s），完成后 toast

## 组件清单（必须创建）

```
src/
├── layouts/
│   └── AppShell.tsx          # sidebar + main 容器，根据断点切换
├── components/
│   ├── SideNav.tsx           # 桌面左侧
│   ├── BottomTabBar.tsx      # 移动底部
│   ├── ConnectionDot.tsx     # 绿/黄/灰 三态圆点
│   ├── QuickInput.tsx        # autosize textarea
│   ├── AtomCard.tsx          # 列表卡片
│   ├── LinkBadge.tsx         # 关联角标
│   ├── LinkSuggest.tsx       # 详情页建议条目
│   ├── SimilarityBar.tsx     # 百分比条
│   ├── EmptyState.tsx        # 通用空态
│   ├── ConfirmDialog.tsx     # 模态确认
│   ├── Toast.tsx + useToast.tsx  # 极简 toast 系统（top-right，自动消失 3s）
│   └── GraphCanvas.tsx       # React Flow 封装
├── lib/
│   ├── mock.ts
│   ├── store.ts
│   ├── useMockWs.ts
│   └── time.ts               # formatRelative('刚刚'/'N 分钟前'/...)
└── pages/  (重写现有 5 个)
```

## 交互细节硬性要求

1. **路由**：用 `react-router-dom` v7（已安装），路径见 `App.tsx` 现有结构
2. **键盘**：Inbox QuickInput 支持 Cmd/Ctrl+Enter 提交，普通 Enter 换行
3. **乐观更新**：所有 mutation（addAtom / confirmLink / ignoreLink）先更新 UI 再 await（mock 都返回 resolved，所以不会回滚，但保留模式）
4. **加载状态**：首次 `loadInitial` 期间显示 skeleton 卡片（3 个灰色占位）
5. **不要全局 CSS**：只允许 `src/index.css` 里那几行 base；新样式全部用 Tailwind 原子类。删除 `src/App.css` 残留
6. **不要 console.log**

## 验证标准（你必须自检通过）

完成后在 `frontend/` 目录下执行：
```bash
pnpm exec tsc --noEmit   # 0 错误
pnpm build               # 成功并保留 PWA 输出
```

无 TypeScript 错误、无 ESLint 阻塞错误（warning 可接受）。

## 提交输出

完成后用一段中文总结：
1. 创建/修改了哪些文件（按路径列出）
2. 任何与本规约的偏差及原因
3. 已知遗留 TODO（如某些动画偷懒了）

不要碰 backend/ 目录。不要碰项目根的 CLAUDE.md / PLAN.md。
